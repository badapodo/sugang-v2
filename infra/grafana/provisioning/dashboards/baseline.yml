apiVersion: 1

providers:
  - name: Baseline
    orgId: 1
    folder: Sugang Baseline
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
